import React from "react";
import AppointmentItems from "./AppointmentItems";

const Appointments = () => {
  return (
    <div>
      <AppointmentItems />
    </div>
  );
};

export default Appointments;
